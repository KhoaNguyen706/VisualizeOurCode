# Optional. Without a key the app still runs: pattern tracers and live
# instrumentation are fully client-side. The key only enables AI-generated
# explanation text and the AI plan tier for unrecognised algorithms.
GEMINI_API_KEY=
